DATABASE_URL=
# Same value as AUTH_SECRET in the Next.js app — used to verify chat JWTs.
AUTH_SECRET=
PORT=8080
# Next.js app origin, e.g. https://corkbored.com (leave empty to allow all in dev)
ALLOWED_ORIGIN=
